import setuptools

setuptools.setup(
    name='quantlib',
    version='0.1',
    description='quantlib by jln',
    url='0',
    author='Justin L. Ng',
    install_requires=['numpy', 'pandas'],
    author_email='',
    packages=setuptools.find_packages(),
    zip_safe=False
)
